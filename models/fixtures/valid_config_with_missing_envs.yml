---
geckoboard_api_key: "{{ NOT_EXISING_KEY }}"
database:
 driver: postgres
 username: "{{ AGAIN INVALID }}"
 password: "{{ NOT-VALID }}"
 host: "{{ }}"
 protocol: "unix"
 name: "{{ INVAL^&ID }}"
refresh_time_sec: 60
datasets:
 - name: some.number
   update_type: replace
   sql: SELECT 124
   fields:
     - type: 'number'
       name: "count"
